{
  "buildCommand": "npm --prefix client run build",
  "outputDirectory": "client/build",
  "rewrites": [
    {
      "source": "/:path((?!.*\\.).*)",
      "destination": "/index.html"
    }
  ],
  "env": {
    "REACT_APP_API_URL": "https://your-server-api.com"
  }
}
